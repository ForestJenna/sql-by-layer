# sql-by-layer
`/home/runner/work/sql-by-layer/sql-by-layer/export_definition_queries.py` is an ArcGIS Pro script-tool ready script that:
- reads all data layers in a specified map inside an `.aprx`
- exports all definition queries (including multiple queries per layer) to one CSV
- includes each query title/name and whether each query is active
